import { SET_USER, SET_TOKEN, SET_AREA_DATA } from './mutation_types'
import api from '../api/index.js'
export default {
	//sendLogin ({ commit }, info) {
	//	const {phone, password, verification} = info
	//	return api.request('get', config.getURL('login/userLogin'), {
	//		phone,
	//		password,
	//		verification
	//	})
	//}
}
